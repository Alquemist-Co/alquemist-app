# Test Plan — Fase 3: Operations & Offline (F-041 a F-050)

## Prerequisitos

- `npm run db:reset` ejecutado exitosamente (seed data cargada, ~1176 environmental readings generadas)
- `npm run dev` corriendo en localhost:3000
- Navegador: Chrome 120+ (para DevTools, PWA, offline simulation, WebSocket inspector)
- Para pruebas offline: DevTools > Network > Offline toggle
- Para pruebas responsive: DevTools Device Toolbar o ventana redimensionable
- Seed data provee: 1 facility (Invernadero Principal, greenhouse, 500 m2), 3 zonas (Propagacion, Vegetativo A, Floracion A), 4 zone structures, 20 plant positions (8 planted, 6 empty, 4 harvested, 2 maintenance), 8 sensors (7 active, 1 inactive), ~1176 environmental readings (7 dias, hourly), 12 alerts (5 pending, 3 acknowledged, 4 resolved), 5 overhead costs, 5 batches (3 active, 1 completed, 1 active split child)

## Credenciales rapidas

| Rol | Email | Password |
|-----|-------|----------|
| admin | admin@agrotech.co | Admin123! |
| supervisor | supervisor@agrotech.co | Super123! |
| operator | operator@agrotech.co | Oper123! |
| manager | manager@agrotech.co | Mgr123! |
| viewer | viewer@agrotech.co | View123! |

---

## F-041: Mapa de Facility y Grid de Zonas

**Contexto**: 1 facility (Invernadero Principal, greenhouse, 500 m2), 3 zonas activas: Sala Propagacion (propagation, 25 m2, cap 200), Sala Vegetativo A (vegetation, 80 m2, cap 100), Sala Floracion A (flowering, 120 m2, cap 80). Batch LOT-GELATO-260301 en Propagacion, LOT-GELATO-260201 en Vegetativo A, LOT-GELATO-260101 + splits en Floracion A. Ruta: `/areas`.

| ID | Descripcion | Prio | Rol | Precondiciones | Pasos | Resultado Esperado |
|----|-------------|------|-----|----------------|-------|--------------------|
| T-3F041-001 | Pagina Areas muestra facility con zone cards | P0 | supervisor | Logueado como supervisor, seed data cargada | 1. Navegar a `/areas`. 2. Verificar que se muestra la facility "Invernadero Principal". 3. Verificar que aparecen 3 zone cards. | La pagina carga sin errores. Se muestra "Invernadero Principal" como facility seleccionada (auto-seleccion por ser unica). Se renderizan 3 zone cards: Sala Propagacion, Sala Vegetativo A, Sala Floracion A. |
| T-3F041-002 | Cada zone card muestra nombre, proposito, area, capacidad y status | P0 | supervisor | Pagina `/areas` cargada | 1. Revisar la zone card "Sala Propagacion". 2. Verificar nombre, badge de proposito, area, capacidad. 3. Repetir para las otras 2 zonas. | Sala Propagacion muestra: nombre, badge "Propagacion" (color verde claro), area 25 m2, capacidad 200 plantas, status "Activa". Sala Vegetativo A: badge "Vegetativo" (verde), 80 m2, cap 100. Sala Floracion A: badge "Floracion" (morado), 120 m2, cap 80. |
| T-3F041-003 | Zone cards usan colores por proposito | P1 | supervisor | Pagina `/areas` cargada | 1. Comparar visualmente el color de fondo o borde de cada zone card. 2. Verificar que propagation, vegetation y flowering tienen colores distintos. | Cada zona tiene un color de fondo/borde diferente segun su purpose: propagation = verde claro, vegetation = verde, flowering = morado. Los colores son facilmente distinguibles entre si. |
| T-3F041-004 | Batch count activo visible en cada zone card | P1 | supervisor | Pagina `/areas` cargada | 1. Verificar que cada zone card muestra la cantidad de batches activos. 2. Contar batches por zona segun seed: Propagacion (1), Vegetativo (1), Floracion (3). | Sala Propagacion muestra 1 batch activo (LOT-GELATO-260301). Sala Vegetativo A muestra 1 batch activo (LOT-GELATO-260201). Sala Floracion A muestra 3 batches (LOT-GELATO-260101 + 2 split children, segun estado). El count refleja batches con status active. |
| T-3F041-005 | Click en zone card navega a zone detail | P1 | supervisor | Pagina `/areas` cargada | 1. Hacer click en la zone card "Sala Vegetativo A". 2. Verificar la navegacion. | El navegador navega a `/areas/{zoneId}` (UUID de zona Vegetativo A). La pagina de detalle de zona carga sin errores. Se puede volver a `/areas` con breadcrumbs o boton back. |
| T-3F041-006 | Stats de facility muestran datos agregados | P2 | manager | Logueado como manager, pagina `/areas` | 1. Localizar la seccion de stats en la parte superior de la pagina. 2. Verificar area total, capacidad total, plantas actuales. | Se muestran stat cards con: Area total (225 m2 = 25 + 80 + 120), Capacidad (380 = 200 + 100 + 80). Plantas actuales refleja la suma de plant_count de batches activos. Los numeros se muestran en tipografia DM Mono. |
| T-3F041-007 | Solo manager/admin/supervisor ven la pagina Areas | P2 | viewer | Logueado como viewer | 1. Navegar directamente a `/areas` en la barra de direcciones. 2. Verificar acceso. | La pagina carga pero viewer tiene acceso de solo lectura. No hay botones de edicion ni acciones de gestion visibles. La informacion se muestra correctamente para consulta. |

---

## F-042: Detalle de Zona con Clima y Batches

**Contexto**: Zona Sala Vegetativo A tiene batch LOT-GELATO-260201 (active, vegetative phase, 40 plantas), 3 sensores activos (temperatura, humedad, CO2), ~504 environmental readings (3 params x 168 hours). Ruta: `/areas/{zoneId}`.

| ID | Descripcion | Prio | Rol | Precondiciones | Pasos | Resultado Esperado |
|----|-------------|------|-----|----------------|-------|--------------------|
| T-3F042-001 | Detalle de zona muestra datos principales | P0 | supervisor | Logueado, seed data cargada | 1. Navegar a `/areas` y click en "Sala Vegetativo A". 2. Verificar header con nombre, proposito, environment, area, capacidad, status. | El header muestra: nombre "Sala Vegetativo A", badge "Vegetativo" (verde), badge "Indoor" (controlled), area "80 m2", capacidad "100 plantas", status badge "Activa" (verde). No hay errores en consola. |
| T-3F042-002 | Seccion de batches muestra batch activo LOT-GELATO-260201 | P0 | supervisor | Detalle de Sala Vegetativo A abierto | 1. Localizar la seccion de batches en la zona. 2. Verificar que aparece LOT-GELATO-260201. 3. Verificar datos del batch: codigo, cultivar, fase, plant count. | Se muestra 1 batch: LOT-GELATO-260201. El batch muestra: codigo (monospace), cultivar "Gelato #41", fase "Vegetativo" (badge coloreado), 40 plantas. Se incluye indicador de dias en fase. |
| T-3F042-003 | Dials de clima muestran lecturas actuales de sensores | P1 | supervisor | Detalle de Sala Vegetativo A abierto | 1. Localizar la seccion de dials/gauges ambientales. 2. Verificar que existen dials para temperatura, humedad y CO2. 3. Verificar que cada dial muestra un valor numerico. | Se muestran 3 dials (DialGauge SVG): temperatura (~22-27 C), humedad (~50-65%), CO2 (~600-1200 ppm). Cada dial tiene valor numerico central, label debajo, y color segun rango (verde si en rango optimo del cultivar Gelato). |
| T-3F042-004 | Dials usan colores segun rango optimo del cultivar | P1 | supervisor | Dials de clima visibles | 1. Observar el color del arco de cada dial. 2. Comparar con rango optimo de Gelato #41: temp 20-26 C, RH 40-60%. 3. Verificar que valores dentro de rango aparecen en verde. | Si la lectura actual esta dentro del rango optimo, el dial se muestra en verde (ok). Si estuviera fuera de rango (ej: temperatura > 26 C), el dial cambiaria a amarillo (warning) o rojo (critical). La banda verde de rango optimo es visible en el arco del dial. |
| T-3F042-005 | Resumen de plant positions muestra conteo por status | P1 | supervisor | Detalle de Sala Vegetativo A abierto | 1. Localizar informacion de posiciones de planta en la zona. 2. Verificar conteos por status. | Se muestra resumen de posiciones: 4 posiciones en Sala Vegetativo A (Mesa Vegetativo 1), todas con status "harvested" segun seed. El conteo refleja la distribucion real de posiciones en la zona. |
| T-3F042-006 | Navegar a batch detail desde la zona | P1 | supervisor | Seccion de batches visible | 1. Hacer click en el batch LOT-GELATO-260201. 2. Verificar la navegacion. | El navegador navega a `/batches/{batchId}`. La pagina de detalle de batch carga mostrando LOT-GELATO-260201. Se puede volver al detalle de zona con breadcrumbs o back. |
| T-3F042-007 | Historial ambiental muestra grafico de 7 dias | P1 | supervisor | Detalle de Sala Vegetativo A abierto | 1. Localizar la seccion de historial ambiental. 2. Verificar que existe un grafico de lineas. 3. Verificar que cubre 7 dias de datos. | Se muestra un grafico de lineas (Recharts LineChart) con lecturas de temperatura, humedad y CO2 de los ultimos 7 dias. El eje X muestra rango temporal de ~7 dias. Cada parametro tiene linea de color distintivo. El grafico es interactivo (tooltip al hover). |
| T-3F042-008 | Stale reading detection si ultimo dato > 30 min | P2 | supervisor | Detalle de zona con sensor sin lecturas recientes | 1. Verificar el dial del sensor inactivo de CO2 en Sala Floracion A (sensor CO2-FLOR-01 inactive). 2. Navegar al detalle de Sala Floracion A. 3. Observar el dial de CO2. | El sensor de CO2 esta inactivo en Floracion A. Si no hay lecturas recientes para CO2 en esa zona, el dial muestra "N/A" o "Sin datos" con indicador de sensor ausente/stale. Se indica la hora de la ultima lectura disponible o "Nunca". |

---

## F-043: Posiciones de Planta (Grid Visual)

**Contexto**: 20 posiciones distribuidas en 4 estructuras. Rack Propagacion A: 8 planted (batch LOT-GELATO-260301) + 6 empty. Rack Propagacion B: 2 empty (no en seed, pero 6 empty total en Prop A). Mesa Vegetativo 1: 4 harvested. Mesa Floracion 1: 2 maintenance. Ruta: `/areas/{zoneId}/positions`.

| ID | Descripcion | Prio | Rol | Precondiciones | Pasos | Resultado Esperado |
|----|-------------|------|-----|----------------|-------|--------------------|
| T-3F043-001 | Grid muestra posiciones con colores por status | P0 | supervisor | Logueado, seed data cargada | 1. Navegar a `/areas` > click "Sala Propagacion" > localizar link a posiciones o navegar a `/areas/{zoneId}/positions` para Propagacion. 2. Verificar que aparece un grid de celdas. 3. Verificar colores por status. | Se muestra un grid de 14 posiciones (Rack Prop A: 8 planted + 6 empty). Las celdas plantadas (8) se muestran en verde. Las celdas vacias (6) se muestran en gris. Los colores son facilmente distinguibles. Las celdas tienen tamano uniforme (32-40px). |
| T-3F043-002 | Posiciones plantadas muestran asociacion con batch | P0 | supervisor | Grid de posiciones de Propagacion abierto | 1. Observar las celdas verdes (planted). 2. Verificar que muestran referencia al batch. | Las 8 posiciones plantadas muestran el codigo del batch LOT-GELATO-260301 (truncado si no cabe). Al hacer hover o tap, se ve la asociacion completa con el batch. Los labels de posicion son visibles (A1-L1-P1, A1-L1-P2, etc.). |
| T-3F043-003 | Grid layout refleja estructura y niveles | P1 | supervisor | Grid de posiciones de Propagacion abierto | 1. Verificar que las posiciones estan agrupadas por estructura (Rack Prop A, Rack Prop B). 2. Verificar que hay separacion visual entre niveles (L1, L2, L3). | Las posiciones se agrupan por estructura con header visible del nombre. Rack Prop A muestra 3 niveles (L1: 4 posiciones, L2: 4 posiciones, L3: 4 posiciones). Rack Prop B muestra 2 posiciones (L1). La agrupacion por nivel es clara visualmente. |
| T-3F043-004 | Labels de posicion en formato correcto | P1 | supervisor | Grid de posiciones visible | 1. Verificar los labels de varias posiciones. 2. Comparar con formato esperado (Estructura-Nivel-Posicion). | Los labels siguen el formato: A1-L1-P1, A1-L1-P2, A1-L2-P1, etc. para Rack A. B1-L1-P1, B1-L1-P2 para Rack B. V1-P1, V1-P2 para Mesa Vegetativo. F1-P1, F1-P2 para Mesa Floracion. El texto se muestra en tipografia monospace (DM Mono). |
| T-3F043-005 | Posiciones empty distinguibles de planted | P1 | supervisor | Grid de posiciones visible | 1. Comparar visualmente celdas planted vs empty. 2. Verificar que la diferencia de color es clara. | Las celdas planted (verde #059669) son claramente diferentes de empty (gris #D4DDD6). La diferencia es obvia a simple vista sin necesidad de hover o leer labels. |
| T-3F043-006 | Posiciones harvested y maintenance con colores distintos | P2 | supervisor | Seed data cargada | 1. Navegar a posiciones de Sala Vegetativo A (4 harvested). 2. Verificar color de celdas harvested. 3. Navegar a posiciones de Sala Floracion A (2 maintenance). 4. Verificar color de celdas maintenance. | Las posiciones harvested (Mesa Veg) se muestran en naranja (#D97706). Las posiciones maintenance (Mesa Flor) se muestran en rojo (#DC2626). Cada status tiene color unico e inconfundible. |
| T-3F043-007 | Click en posicion muestra detalle con link a batch | P2 | supervisor | Grid de posiciones Propagacion abierto | 1. Hacer click/tap en una posicion plantada (ej: A1-L1-P1). 2. Verificar que se abre un detalle (tooltip, bottom sheet o panel). 3. Verificar los datos mostrados. | Se muestra detalle de la posicion: label A1-L1-P1, estructura Rack Propagacion A, nivel 1, status "Plantada", batch LOT-GELATO-260301, cultivar Gelato #41. Se incluye boton o link "Ver batch" que navega al detalle del batch. |

---

## F-044: Ocupacion Planificada (Gantt)

**Contexto**: 5 batches en 3 zonas. LOT-GELATO-260301 (germination, Propagacion, start 2026-03-01). LOT-GELATO-260201 (vegetative, Vegetativo A, start 2026-02-01, end 2026-06-01). LOT-GELATO-260101 + splits (drying, Floracion A, start 2026-01-01, end 2026-04-15). Ruta: `/areas/occupancy`.

| ID | Descripcion | Prio | Rol | Precondiciones | Pasos | Resultado Esperado |
|----|-------------|------|-----|----------------|-------|--------------------|
| T-3F044-001 | Gantt chart renderiza barras horizontales para batches | P0 | manager | Logueado como manager, seed data cargada | 1. Navegar a `/areas/occupancy`. 2. Verificar que se muestra un diagrama Gantt con barras horizontales. 3. Contar las barras visibles. | El Gantt renderiza correctamente con SVG. Las filas representan zonas (eje Y) y el tiempo esta en el eje X. Se ven barras para los batches activos en sus zonas correspondientes. Cada barra muestra informacion del batch (codigo, cultivar). |
| T-3F044-002 | Barras coloreadas por fase de produccion | P0 | manager | Gantt visible | 1. Observar los colores de las barras. 2. Comparar con las fases de cada batch: germination (LOT-260301), vegetative (LOT-260201), drying (LOT-260101). | Cada barra tiene un color diferente segun la fase actual del batch. Los colores corresponden a los definidos para cada phase badge (germination, vegetative, drying, etc.). Las barras tienen border-radius y son visualmente claras. |
| T-3F044-003 | Timeline cubre rango de fechas correcto | P1 | manager | Gantt visible | 1. Verificar las fechas del eje X. 2. Confirmar que el rango incluye todos los batches visibles. 3. Verificar que la linea de "hoy" esta visible. | El eje X muestra semanas con headers en DM Mono. El rango temporal cubre desde la fecha mas antigua (2026-01-01 para LOT-260101) hasta la mas futura (2026-06-01 para LOT-260201). Se muestra una linea vertical roja para el dia actual. |
| T-3F044-004 | Overlap detection en zonas con multiples batches | P1 | manager | Gantt visible | 1. Observar la zona Sala Floracion A. 2. Verificar que muestra LOT-GELATO-260101 y sus 2 splits (A y B). 3. Verificar si hay indicador de solapamiento. | Sala Floracion A muestra multiples barras (batch padre + split children). Las barras solapadas se apilan verticalmente dentro de la fila de la zona o se muestran con indicador de multiples batches. Si hay solapamiento, se indica con warning visual. |
| T-3F044-005 | Cada zona aparece en fila separada | P1 | manager | Gantt visible | 1. Verificar que las 3 zonas aparecen como filas independientes. 2. Verificar que los nombres de zona son visibles (sticky left). | Las 3 zonas (Propagacion, Vegetativo A, Floracion A) tienen filas separadas con sus nombres en el lado izquierdo. Los nombres permanecen visibles al hacer scroll horizontal (sticky). Las filas tienen altura uniforme (~48px). |
| T-3F044-006 | Click en barra navega a detalle de batch | P2 | manager | Gantt visible | 1. Hacer click en la barra de LOT-GELATO-260201. 2. Verificar que se muestra tooltip o detalle. 3. Si hay boton "Ver detalle", hacer click. | Al hacer click en una barra, se muestra tooltip (desktop) o bottom sheet (mobile) con: codigo, cultivar, fase, plant_count, zona, fechas. Un boton "Ver detalle" navega a `/batches/{batchId}`. Al hover (desktop), se muestra preview rapido. |

---

## F-045: Monitoreo Ambiental Real-Time

**Contexto**: ~1176 readings (7 sensors x ~168 horas), 3 zonas con sensores. Propagacion: temp + humidity. Vegetativo A: temp + humidity + CO2. Floracion A: temp + humidity + CO2 (CO2 inactive). Componente DialGauge SVG 270 grados. Ruta: `/operations/environment`.

| ID | Descripcion | Prio | Rol | Precondiciones | Pasos | Resultado Esperado |
|----|-------------|------|-----|----------------|-------|--------------------|
| T-3F045-001 | Dashboard muestra cards con lecturas por zona | P0 | supervisor | Logueado como supervisor, seed data cargada | 1. Navegar a `/operations/environment`. 2. Verificar que se muestran cards por zona. 3. Verificar que cada card tiene dials circulares. | Se muestran 3 cards (una por zona con sensores activos): Propagacion, Vegetativo A, Floracion A. Cada card muestra el nombre de la zona y dials con valores actuales. Layout: 1 columna mobile, 2-3 columnas desktop. |
| T-3F045-002 | DialGauge muestra valor y rango correcto | P0 | supervisor | Dashboard ambiental abierto | 1. Observar el dial de temperatura de Sala Vegetativo A. 2. Verificar que muestra un valor numerico (~22-27 C). 3. Verificar que el arco SVG es de 270 grados. 4. Verificar la banda de rango optimo. | El DialGauge renderiza un arco SVG de 270 grados. El valor numerico se muestra en el centro (DM Mono Bold). La banda verde del rango optimo es visible (20-26 C para Gelato). El dial tiene label descriptivo debajo ("Temperatura", "Humedad", "CO2"). |
| T-3F045-003 | Color coding: ok (verde), warning (amarillo), critical (rojo) | P1 | supervisor | Dashboard ambiental abierto | 1. Observar los colores de los dials. 2. Verificar que valores dentro de rango estan en verde. 3. Verificar que la logica de color es: < 5% desviacion = verde, 5-10% = amarillo, > 10% = rojo. | Los dials dentro del rango optimo se muestran en verde. Si alguna lectura random cayo fuera del rango (posible con datos seed random), ese dial se muestra en amarillo o rojo segun la magnitud de la desviacion. La codificacion de color es consistente. |
| T-3F045-004 | Stale reading detection (lectura > 30 min) | P1 | supervisor | Dashboard ambiental abierto | 1. Verificar si algun dial muestra indicador de lectura "stale". 2. Si las lecturas seed son recientes (generadas hasta CURRENT_TIMESTAMP), no deberia haber stale. 3. Verificar el timestamp de ultima lectura. | Cada card muestra timestamp de la ultima lectura ("Actualizado hace X min"). Si las lecturas son recientes (< 30 min), el status es normal. Si alguna lectura tiene > 30 min, se marca como stale con color gris y texto "Sin datos recientes". |
| T-3F045-005 | Grafico historico de 7 dias visible | P1 | supervisor | Dashboard ambiental abierto o detalle de zona | 1. Localizar el grafico de historial (puede estar en detalle de zona). 2. Verificar que cubre 7 dias. 3. Verificar que muestra temperatura, humedad y CO2. | Se muestra un LineChart (Recharts) con datos de los ultimos 7 dias. Cada parametro tiene linea con color distintivo (temp=rojo, humidity=cyan, CO2=gris-verde). El eje X muestra dias/horas. Tooltip al hover con valor exacto y timestamp. |
| T-3F045-006 | Parametros temperatura, humedad, CO2 mostrados | P1 | supervisor | Dashboard ambiental abierto | 1. Verificar que la card de Vegetativo A tiene 3 dials (temp, humidity, CO2). 2. Verificar que Propagacion tiene 2 dials (temp, humidity — no tiene CO2). 3. Verificar que Floracion A muestra N/A para CO2 (sensor inactivo). | Vegetativo A: 3 dials completos. Propagacion: 2 dials (sin CO2). Floracion A: 2 dials activos (temp, humidity) y CO2 muestra "N/A" o ausente (sensor CO2-FLOR-01 esta inactivo). Los parametros faltantes no generan errores. |
| T-3F045-007 | Banda de rango optimo visible en gauge | P2 | supervisor | Dashboard ambiental abierto | 1. Observar un dial de temperatura que tenga cultivar asociado (zona con batches activos). 2. Verificar que se ve una banda verde en el arco del dial. | El arco del DialGauge muestra una banda verde semitransparente en la zona correspondiente al rango optimo del cultivar (20-26 C para temperatura con Gelato). La banda es visible pero no oculta el arco principal. |
| T-3F045-008 | Click en card de zona navega a zone detail | P2 | supervisor | Dashboard ambiental abierto | 1. Hacer click en la card de "Sala Vegetativo A". 2. Verificar navegacion. | El navegador navega a `/areas/{zoneId}` del detalle de esa zona. La pagina de detalle carga con dials de clima expandidos y mas informacion de la zona. |

---

## F-046: Gestion de Sensores

**Contexto**: 8 sensores en 3 zonas. Propagacion: SHT-PROP-T01 (temp), SHT-PROP-H01 (humidity). Vegetativo: SHT-VEG-T01 (temp), SHT-VEG-H01 (humidity), CO2-VEG-01 (co2). Floracion: SHT-FLOR-T01 (temp), SHT-FLOR-H01 (humidity), CO2-FLOR-01 (co2, INACTIVE). Calibracion: 10-30 dias. Ruta: `/operations/sensors`.

| ID | Descripcion | Prio | Rol | Precondiciones | Pasos | Resultado Esperado |
|----|-------------|------|-----|----------------|-------|--------------------|
| T-3F046-001 | Lista de sensores muestra todos 8 sensores | P0 | admin | Logueado como admin, seed data cargada | 1. Navegar a `/operations/sensors`. 2. Contar los sensores listados. 3. Verificar columnas: zona, tipo, marca/modelo, serial, estado. | Se muestran 8 sensores en tabla (desktop) o cards (mobile). Cada sensor muestra: zona asignada, tipo (temperature/humidity/co2), marca/modelo (SHT40-AD1B o MH-Z19C), serial, estado activo/inactivo. Los datos son correctos segun seed. |
| T-3F046-002 | Sensor inactivo CO2-FLOR-01 claramente marcado | P0 | admin | Lista de sensores abierta | 1. Localizar el sensor CO2-FLOR-01. 2. Verificar indicador de estado inactivo. | El sensor CO2-FLOR-01 (co2, Sala Floracion A) se muestra con indicador gris (inactivo). Tiene un dot gris o badge "Inactivo" claramente visible. Los 7 sensores restantes muestran indicador verde (activo). |
| T-3F046-003 | Crear nuevo sensor con formulario | P1 | admin | Lista de sensores abierta | 1. Hacer click en "Registrar sensor" o boton equivalente. 2. Llenar: zona = Sala Propagacion, tipo = co2, marca = MH-Z19C, serial = CO2-PROP-01. 3. Enviar formulario. | Se abre un dialog/modal con formulario. Al enviar, el sensor se crea exitosamente. Se muestra toast "Sensor registrado correctamente". El nuevo sensor aparece en la lista con indicador verde (activo por defecto). |
| T-3F046-004 | Editar sensor: cambiar calibracion y activar/desactivar | P1 | admin | Al menos 1 sensor existe | 1. Hacer click en el sensor CO2-FLOR-01 para editar. 2. Cambiar calibration_date a hoy. 3. Marcar is_active = true. 4. Guardar cambios. | El formulario de edicion carga con datos actuales. Al guardar, el sensor se actualiza: nueva fecha de calibracion visible, indicador cambia de gris (inactivo) a verde (activo). Toast de confirmacion. |
| T-3F046-005 | Filtrar sensores por zona | P1 | admin | Lista con 8+ sensores | 1. Localizar el filtro de zona. 2. Seleccionar "Sala Vegetativo A". 3. Verificar resultados. | La lista se filtra mostrando solo 3 sensores de Vegetativo A: SHT-VEG-T01 (temp), SHT-VEG-H01 (humidity), CO2-VEG-01 (co2). Los sensores de otras zonas se ocultan. Al limpiar filtro, vuelven los 8. |
| T-3F046-006 | Indicador de calibracion vigente/vencida | P2 | admin | Lista de sensores visible | 1. Verificar el indicador de calibracion de cada sensor. 2. Los sensores seed tienen calibracion entre 10 y 30 dias atras (vigente si umbral es 90 dias). | Todos los sensores muestran calibracion vigente (verde) ya que las fechas son recientes (10-30 dias). El indicador muestra "Calibrado: {fecha}" al hover. Si la calibracion hubiera vencido, se mostraria en rojo con badge "Vencida". |
| T-3F046-007 | Solo supervisor/manager/admin pueden gestionar sensores | P2 | operator | Logueado como operator | 1. Navegar a `/operations/sensors`. 2. Verificar si aparecen botones de crear/editar. | El operator puede ver la lista de sensores (solo lectura). No aparecen botones "Registrar sensor" ni opciones de edicion. Los formularios de creacion/edicion no son accesibles para el rol operator. |

---

## F-047: Centro de Alertas

**Contexto**: 12 alertas seed. 5 pending: aa01 (overdue_activity, warning), aa02 (overdue_activity, warning), aa03 (low_inventory, warning), aa04 (expiring_item, info), aa05 (quality_failed, critical). 3 acknowledged: aa06 (env_out_of_range, critical), aa07 (env_out_of_range, warning), aa08 (low_inventory, warning). 4 resolved: aa09, aa10, aa11, aa12. Ruta: `/operations/alerts`.

| ID | Descripcion | Prio | Rol | Precondiciones | Pasos | Resultado Esperado |
|----|-------------|------|-----|----------------|-------|--------------------|
| T-3F047-001 | Centro de alertas muestra 3 tabs con conteos | P0 | supervisor | Logueado como supervisor, seed data cargada | 1. Navegar a `/operations/alerts`. 2. Verificar que existen 3 tabs. 3. Verificar los badges con conteo en cada tab. | Se muestran 3 tabs: "Pendientes (5)", "Reconocidas (3)", "Resueltas (4)". El tab "Pendientes" esta activo por defecto. Los badges muestran el conteo correcto de alertas en cada estado. |
| T-3F047-002 | Tab pendientes muestra 5 alertas ordenadas por severidad | P0 | supervisor | Centro de alertas abierto, tab Pendientes activo | 1. Verificar que aparecen 5 alertas. 2. Verificar el orden de las alertas. 3. Verificar que cada alerta muestra tipo, mensaje, severidad, timestamp. | 5 alertas visibles. Ordenadas: primero la critical (quality_failed aa05), luego warnings (overdue_activity aa01, aa02, low_inventory aa03), luego info (expiring_item aa04). Cada alerta muestra icono de severidad, mensaje descriptivo, y timestamp. |
| T-3F047-003 | Reconocer una alerta pendiente | P0 | supervisor | Tab Pendientes con 5 alertas | 1. Localizar la alerta aa03 (low_inventory, CaNO3). 2. Hacer click en boton "Reconocer". 3. Verificar el cambio de estado. | La alerta se mueve al tab "Reconocidas". El badge de "Pendientes" baja a 4. El badge de "Reconocidas" sube a 4. La alerta ya no aparece en el tab Pendientes. Toast o feedback visual confirma la accion. |
| T-3F047-004 | Resolver una alerta reconocida con nota | P0 | supervisor | Tab Reconocidas con al menos 1 alerta | 1. Cambiar al tab "Reconocidas". 2. Localizar una alerta (ej: aa06 env_out_of_range). 3. Hacer click en "Resolver". 4. Ingresar nota de resolucion (ej: "Ventilacion ajustada, temperatura normalizada"). 5. Confirmar. | Se abre un dialog para ingresar resolution_notes (textarea, obligatorio min 5 chars). Al confirmar, la alerta se mueve al tab "Resueltas". Badges se actualizan. Toast confirma "Alerta resuelta". |
| T-3F047-005 | Iconos y colores de severidad correctos | P1 | supervisor | Centro de alertas abierto | 1. Observar los iconos y colores de borde de cada alerta. 2. Comparar: info=azul, warning=amarillo, critical=rojo. | Alerta aa04 (info, expiring_item): borde azul (#0891B2), icono de informacion. Alertas aa01, aa02, aa03 (warning): borde amarillo (#D97706), icono de advertencia. Alerta aa05 (critical, quality_failed): borde rojo (#DC2626), icono de error. Los colores son consistentes y distinguibles. |
| T-3F047-006 | Alerta quality_failed (aa05) aparece como critical | P1 | supervisor | Tab Pendientes abierto | 1. Localizar la alerta "Test de microbiologia FALLO — Batch LOT-GELATO-260101 excede mohos". 2. Verificar severidad y posicion. | La alerta aa05 aparece primera en la lista (critical ordenado primero). Tiene borde rojo, icono de error/alerta critica. El mensaje completo es legible. Muestra severidad "critical" con estilo destacado. |
| T-3F047-007 | Link a entidad relacionada funciona | P1 | supervisor | Centro de alertas abierto | 1. Localizar una alerta con link a entidad (ej: aa01 con entity_type=scheduled_activity). 2. Hacer click en el link o nombre de la entidad. | Al hacer click en la referencia a la entidad, el navegador navega a la pagina correspondiente. Para alertas de tipo batch, navega a batch-detail. Para alertas de sensor, navega a la zona o sensor. Para alertas de product/inventory, navega a inventario. |
| T-3F047-008 | Filtrar alertas por tipo | P2 | supervisor | Centro de alertas con multiples alertas | 1. Localizar filtros de tipo. 2. Seleccionar tipo "overdue_activity". 3. Verificar que solo aparecen las alertas de ese tipo. | Al filtrar por "overdue_activity", se muestran solo 2 alertas (aa01, aa02) en Pendientes. Las demas alertas se ocultan. Al limpiar filtro, vuelven todas. Los filtros aplican dentro del tab activo. |
| T-3F047-009 | Operator puede reconocer pero no resolver | P2 | operator | Logueado como operator, centro de alertas abierto | 1. Verificar que el operator ve las alertas. 2. Intentar reconocer una alerta pendiente. 3. Intentar resolver una alerta reconocida. | El operator puede ver las alertas (lectura). Si tiene permiso, puede reconocer alertas (boton "Reconocer" visible). El boton "Resolver" puede estar oculto o deshabilitado para operator (depende de la implementacion de permisos). Verificar segun configuracion de roles. |

---

## F-048: Costos Overhead y COGS

**Contexto**: 5 overhead costs: oo01 (energy, 4.5M COP, per_m2), oo02 (rent, 8M COP, per_m2), oo03 (labor_fixed, 12M COP, per_plant), oo04 (maintenance, 1.2M COP, per_zone, zona Floracion), oo05 (insurance, 3.6M COP, even_split). Todos para facility Invernadero Principal. Ruta: `/operations/costs`.

| ID | Descripcion | Prio | Rol | Precondiciones | Pasos | Resultado Esperado |
|----|-------------|------|-----|----------------|-------|--------------------|
| T-3F048-001 | Lista de costos muestra 5 entradas | P0 | manager | Logueado como manager, seed data cargada | 1. Navegar a `/operations/costs`. 2. Contar las entradas de costos. 3. Verificar que se muestran datos de cada costo. | Se muestran 5 overhead costs en tabla o cards. Cada entrada muestra: tipo (energy, rent, labor_fixed, maintenance, insurance), descripcion, monto en COP, periodo, base de asignacion. Los montos se formatean como moneda (ej: $4,500,000). |
| T-3F048-002 | Crear nuevo overhead cost | P0 | manager | Pagina de costos abierta | 1. Hacer click en "Registrar costo" o boton equivalente. 2. Llenar: facility = Invernadero Principal, tipo = energy, descripcion = "Gas propano Marzo 2026", monto = 1800000, periodo = 2026-03-01 a 2026-03-31, base = per_m2. 3. Enviar. | Se abre dialog/modal con formulario. Todos los campos se validan (monto positivo, periodo valido). Al enviar, el costo se crea y aparece en la lista. Toast confirma "Costo registrado". El monto se muestra formateado como $1,800,000 COP. |
| T-3F048-003 | Opciones de allocation basis visibles | P1 | manager | Formulario de nuevo costo abierto | 1. Localizar el campo "Base de asignacion". 2. Verificar las opciones disponibles. | Se muestran al menos 4 opciones: per_m2 (por metro cuadrado), per_plant (por planta), per_zone (por zona), even_split (reparto equitativo). Cada opcion tiene una descripcion breve de como se distribuye el costo. |
| T-3F048-004 | COGS calculation en batch detail muestra materiales + labor + overhead | P1 | manager | Batch LOT-GELATO-260201 tiene actividades ejecutadas y overhead asignado | 1. Navegar a `/batches/{id}` del batch LOT-GELATO-260201. 2. Localizar el tab "Costos" o seccion de COGS. 3. Verificar el desglose. | Se muestra desglose de COGS: materiales directos (suma de cost_total de activity_resources e inventory_transactions), labor (horas de actividades * tarifa), overhead (monto prorrateado de overhead_costs). El total es la suma de los tres componentes. Se muestra costo por planta (total / plant_count). |
| T-3F048-005 | Editar y eliminar overhead cost | P1 | manager | Lista de costos con entradas existentes | 1. Hacer click en editar un costo existente (ej: oo01 Electricidad). 2. Cambiar el monto a 5000000. 3. Guardar. 4. Verificar actualizacion. | El formulario carga con datos actuales. Al guardar, el monto se actualiza a $5,000,000. La lista refleja el cambio inmediatamente. Si existe opcion de eliminar, solicita confirmacion antes de borrar. |
| T-3F048-006 | Filtrado por periodo | P2 | manager | Lista de costos con entradas de diferentes periodos | 1. Localizar filtro de periodo. 2. Filtrar por Febrero 2026. 3. Verificar resultados. | Se muestran 4 costos de Febrero 2026 (oo01-oo04). El costo oo05 (insurance, Q1 2026 = Enero-Marzo) puede o no aparecer dependiendo de si el filtro es exacto o por solapamiento. |
| T-3F048-007 | Solo manager/admin pueden gestionar costos | P2 | supervisor | Logueado como supervisor | 1. Navegar a `/operations/costs`. 2. Verificar si puede ver la lista. 3. Verificar si puede crear/editar costos. | El supervisor puede ver la lista de costos (lectura). No aparece boton "Registrar costo". No hay opciones de edicion/eliminacion. Solo manager y admin tienen acceso a crear/editar overhead costs. |

---

## F-049: Offline Completo

**Contexto**: Dexie.js con 8 tablas cacheadas (scheduledActivities, batches, activityTemplates, products, inventoryItems, zones, cultivars, productionPhases) + syncQueue + offlinePhotos + meta. Sync FIFO con backoff exponencial (1s, 4s, 16s), 3 reintentos max. Pre-carga al login. Indicador permanente de conexion (OfflineBanner).

| ID | Descripcion | Prio | Rol | Precondiciones | Pasos | Resultado Esperado |
|----|-------------|------|-----|----------------|-------|--------------------|
| T-3F049-001 | App carga datos cacheados cuando offline | P0 | operator | Logueado como operator con datos pre-cargados (sesion previa online) | 1. Navegar a la app online y verificar que los datos cargan (actividades, batches, etc.). 2. Abrir DevTools > Application > IndexedDB > verificar que AlquemistDB tiene datos. 3. Ir a DevTools > Network > marcar "Offline". 4. Navegar a otra pagina (ej: actividades de hoy, batches). | La app muestra datos cacheados desde IndexedDB sin errores. Las paginas cargan con los datos que estaban disponibles en la ultima sincronizacion. El banner de estado muestra "Sin conexion — datos de las HH:MM". No hay pantallas en blanco ni errores de red no manejados. |
| T-3F049-002 | Acciones offline se encolan y sincronizan al reconectar | P0 | operator | App online con datos cacheados, sabe como simular offline | 1. Marcar "Offline" en DevTools. 2. Ejecutar una accion (ej: completar una actividad o registrar observacion). 3. Verificar que la accion se guarda localmente (feedback optimista). 4. Verificar badge "1 pendiente de sincronizar". 5. Desmarcar "Offline". 6. Observar la sincronizacion automatica. | En offline: la accion se encola en syncQueue (IndexedDB). La UI muestra feedback optimista (la accion aparece como completada). Se muestra badge con conteo de pendientes. Al reconectar: la cola se procesa automaticamente. Toast confirma "1 accion sincronizada correctamente". El badge desaparece. |
| T-3F049-003 | Sync queue procesa con backoff exponencial al fallar | P1 | operator | Acciones encoladas offline | 1. Encolar una accion offline. 2. Reconectar pero simular error de servidor (puede requerir manipulacion o simplemente observar el log). 3. Verificar los reintentos. | La sync queue reintenta con backoff: primer retry despues de ~1s, segundo despues de ~4s, tercero despues de ~16s. Despues de 3 intentos fallidos, el item se marca como "failed" y se notifica al usuario. Items fallidos no bloquean el procesamiento de los siguientes items en la cola. |
| T-3F049-004 | Pre-carga al login descarga tablas clave a IndexedDB | P1 | operator | IndexedDB limpio (primera sesion) | 1. Borrar IndexedDB de la app en DevTools > Application > IndexedDB > Delete database. 2. Login como operator@agrotech.co (online). 3. Observar la descarga de datos (indicador de progreso si existe). 4. Verificar IndexedDB despues del login. | Al iniciar sesion, se descargan datos a IndexedDB: scheduledActivities (actividades proximas), batches (activos), products (catalogo), zones (de la facility), cultivars, productionPhases. Se muestra timestamp de ultima sincronizacion. La app es funcional inmediatamente. |
| T-3F049-005 | Conflicto (409) manejado correctamente en sync | P2 | operator | Accion offline que conflicta con cambio de otro usuario | 1. Encolar una accion offline (ej: completar actividad). 2. Mientras offline, otro usuario completa la misma actividad desde otro dispositivo (puede simularse via SQL directo). 3. Reconectar y observar la sincronizacion. | El server retorna 409 Conflict. El item de sync queue se marca como status "conflict". Se muestra notificacion al usuario indicando el conflicto (ej: "La actividad ya fue completada por otro usuario"). El conflicto no bloquea otras acciones en la cola. |
| T-3F049-006 | Indicador offline muestra estado de sync | P2 | operator | App abierta | 1. Verificar banner de estado cuando online: "Conectado" (verde). 2. Marcar "Offline": verificar banner "Sin conexion" (amarillo). 3. Encolar 2 acciones offline: verificar "Sin conexion — 2 acciones pendientes". 4. Reconectar: verificar "Sincronizando..." y luego "Conectado". | El banner OfflineBanner (28px, aria-live) cambia entre estados: Conectado (verde), Sin conexion (amarillo con timestamp), Sincronizando (azul con progreso), y vuelta a Conectado. Las transiciones son fluidas (200ms). El banner es visible en todas las pantallas dentro del AppShell. |

---

## F-050: Supabase Realtime

**Contexto**: Channel manager con dedup por nombre. 4 hooks: useRealtimeAlerts (canal alerts:{companyId}), useRealtimeBatches (batches:{companyId}), useRealtimeActivities (activities:{userId}), useRealtimeEnv (env:{zoneId}). RealtimeProvider en AppShell. Canales por rol: operator = alerts + activities, supervisor = todos, manager = alerts + batches, viewer = batches.

| ID | Descripcion | Prio | Rol | Precondiciones | Pasos | Resultado Esperado |
|----|-------------|------|-----|----------------|-------|--------------------|
| T-3F050-001 | Nueva alerta aparece en real-time sin refresh | P0 | supervisor | Logueado como supervisor, centro de alertas abierto (`/operations/alerts`) | 1. Abrir `/operations/alerts` en tab A. 2. En otra tab (o via SQL), insertar una nueva alerta: `INSERT INTO alerts (id, type, severity, entity_type, entity_id, message, triggered_at, company_id) VALUES (gen_random_uuid(), 'env_out_of_range', 'critical', 'sensor', 'ffffffff-ffff-ffff-ffff-ffffffffffff03', 'TEST: Temperatura alta', NOW(), '11111111-1111-1111-1111-111111111111');`. 3. Volver a tab A y observar. | La nueva alerta aparece automaticamente en el tab "Pendientes" sin necesidad de refrescar la pagina. El badge de conteo se incrementa. La alerta entra con animacion de slide-in. Si es critical, puede mostrarse un toast adicional. Tiempo de aparicion < 5 segundos. |
| T-3F050-002 | Cambio de status de batch se refleja en real-time en batch list | P1 | supervisor | Logueado como supervisor, lista de batches abierta (`/batches`) | 1. Abrir `/batches` en tab A. 2. En otra tab o via SQL, actualizar un batch: `UPDATE batches SET status = 'completed' WHERE id = 'cccccccc-cccc-cccc-cccc-cccccccccc04';`. 3. Volver a tab A y observar. | La lista de batches se actualiza automaticamente. El batch LOT-GELATO-260101-A cambia su badge de status a "Completado". Si la lista esta filtrada por "activos", el batch desaparece con animacion. No se requiere refresh manual. |
| T-3F050-003 | Completion de actividad actualiza lista de actividades del dia | P1 | operator | Logueado como operator, pagina de actividades abierta | 1. Abrir la pagina de actividades de hoy. 2. En otra sesion, completar una actividad asignada al operator (via otra tab o usuario). 3. Observar la lista en la tab original. | La actividad completada se actualiza en la lista automaticamente. El status cambia de "Pendiente" a "Completada". El conteo de actividades pendientes se actualiza. La actualizacion ocurre sin refresh. |
| T-3F050-004 | Channel unsubscribe al navegar fuera de la pagina | P2 | supervisor | App abierta con DevTools | 1. Navegar a `/operations/alerts` (suscribe canal alerts:{companyId}). 2. Abrir DevTools > Network > WS tab y verificar la conexion WebSocket activa. 3. Navegar a otra pagina (ej: `/batches`). 4. Verificar que el canal anterior se desuscribe correctamente. | Al navegar fuera de la pagina de alertas, el canal se desuscribe (cleanup de useEffect). No quedan suscripciones huerfanas. Al navegar de vuelta a alertas, se crea una nueva suscripcion. No hay memory leaks (verificable en DevTools Memory). |
| T-3F050-005 | Multiples tabs no duplican channels | P2 | supervisor | App abierta en 2 tabs | 1. Abrir `/operations/alerts` en tab A. 2. Abrir `/operations/alerts` en tab B. 3. Insertar una nueva alerta via SQL. 4. Verificar que ambas tabs reciben la alerta. 5. Verificar en DevTools que no hay conexiones duplicadas innecesarias dentro de cada tab. | Ambas tabs reciben la nueva alerta independientemente (cada tab tiene su propia conexion WebSocket). Dentro de una misma tab, el channel manager evita suscripciones duplicadas al mismo canal. La alerta aparece exactamente 1 vez en cada tab. |

---

## Resumen de Cobertura

| Feature | P0 | P1 | P2 | Total |
|---------|----|----|----|----|
| F-041 Facility Map & Zone Grid | 2 | 3 | 2 | 7 |
| F-042 Zone Detail | 2 | 4 | 2 | 8 |
| F-043 Plant Positions Grid | 2 | 3 | 2 | 7 |
| F-044 Occupancy Gantt | 2 | 3 | 1 | 6 |
| F-045 Environmental Monitoring | 2 | 4 | 2 | 8 |
| F-046 Sensor Management | 2 | 3 | 2 | 7 |
| F-047 Alert Center | 4 | 3 | 2 | 9 |
| F-048 Overhead Costs & COGS | 2 | 3 | 2 | 7 |
| F-049 Offline Complete | 2 | 2 | 2 | 6 |
| F-050 Supabase Realtime | 1 | 2 | 2 | 5 |
| **Total** | **21** | **30** | **19** | **70** |
